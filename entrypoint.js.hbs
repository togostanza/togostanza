import Stanza from './stanza.js';

const templates = {
  {{#each templates as |template|}}
    "{{template.name}}": {{template.spec}}
  {{/each}}
};

const metadata = {{metadataJSON}};
const outer    = {{outerJSON}};

const init = {{script}};

init({templates, metadata, outer});
