<!DOCTYPE html>
<html>
  <head> </head>

  <body>
    <ul>
      <% Object.entries(stanzas).forEach(([stanza, path]) => { %>
      <li><%= stanza %> <%= path %></li>
      <% }); %>
    </ul>
  </body>
</html>
